[broker]
broker_id = 1
cluster_id = "datacore-test-memory"
host = "0.0.0.0"
port = 9092
max_connections = 1000

[storage]
engine = "memory"

[storage.memory]
max_memory_mb = 1024
segment_size_bytes = 104857600

[rest]
enabled = true
host = "0.0.0.0"
port = 8080
